<UserControl x:Class="YOS.ViewPortControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:hx="http://helix-toolkit.org/wpf/SharpDX"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:YOS"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <hx:Viewport3DX ShowViewCube="True"
                        Camera="{Binding Camera, ElementName=Cam}">
            <hx:Viewport3DX.InputBindings>
                <MouseBinding Command="hx:ViewportCommands.Rotate" Gesture="RightClick" />
                <MouseBinding Command="hx:ViewportCommands.Pan" Gesture="MiddleClick" />
            </hx:Viewport3DX.InputBindings>
            <hx:DirectionalLight3D Color="White" Direction="{Binding cam.LookDirection}"/>
            <hx:CameraModel3D x:Name="Cam">
                <hx:CameraModel3D.Camera>
                    <hx:PerspectiveCamera/>
                </hx:CameraModel3D.Camera>
            </hx:CameraModel3D>
            <hx:MeshGeometryModel3D>
               
            </hx:MeshGeometryModel3D>
        </hx:Viewport3DX>

    </Grid>
</UserControl>
